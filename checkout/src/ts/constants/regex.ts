/**
 * @constant
 * @description Regex for string validation that contains only numbers. You can also use it to remove letters.
 * @version 1.0.0
 */
export const ONLY_NUMBERS_REGEX = /\D/g

/**
 * @constant
 * @description Regex for string validation that contains only letters. You can also use it to remove numbers.
 * @version 1.0.0
 */
export const ONLY_LETTERS_REGEX = /[^a-zA-Z]+/g
