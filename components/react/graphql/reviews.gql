query reviewsByProductId($productId: String!) {
  reviewsByProductId(productId: $productId, to: 100, from: 0) {
    data {
      id
      rating
      title
      text
      reviewerName
      reviewDateTime
    }
    range {
      total
      from
      to
    }
  }
}
